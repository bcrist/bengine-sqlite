
///////////////////////////////////////////////////////////////////////////////
enum class `typename` {`
   with each constants using # {
      if (__k__ > 1) { `,` }
      nl `   ` $[1]
   }`
};

const char* get_name(`typename` `param`);
`typename` get_result_type(int `param`);
